# План улучшений системы заданий и управления группами курсов

**Дата**: 2025-12-11
**Статус**: В работе

## Описание задачи

Реализация улучшений в системе заданий и добавление функционала управления группами курсов для методиста.

## Требования

### 1. Изменения в заданиях

#### 1.1. Переименование полей
- **Было**: `description` (Описание)
- **Стало**: `short_description` (Краткое описание)
- **Где отображать**: Во всех карточках предпросмотра заданий (списки, дашборды)

#### 1.2. Новое поле "Подробная инструкция"
- **Поле**: `detailed_instruction` (Text/HTML)
- **Дополнительно**:
  - `instruction_videos` (JSON) - массив ссылок на видео
  - `instruction_files` (JSON) - массив файлов для скачивания
- **Где**: Форма создания/редактирования задания у методиста
- **Отображение**: На странице просмотра задания у студента/преподавателя

#### 1.3. Удаление "Тип сдачи"
- **Удалить поле**: `submission_type` из всех форм
- **Где удалить**:
  - Форма создания/редактирования задания (методист)
  - Форма просмотра задания (преподаватель)
  - Форма сдачи задания (студент)
- **Оставить в модели данных**: Поле остается в `assignment_template`, но скрыто из UI

### 2. Управление группами курсов у методиста

#### 2.1. Новый пункт меню "Группы курса"
- **Расположение**: В меню "Разделы шаблона" при редактировании курса
- **Содержимое**:
  - Список всех групп курса (course_instance) для данного шаблона
  - Фильтрация по статусу: Запланированные / Активные / Завершенные
  - Кнопка "Запустить курс"

#### 2.2. Модалка "Запустить курс"
- **Поля**:
  - Название группы (опционально)
  - Дата начала (обязательно)
  - Дата окончания (обязательно)
  - Преподаватель (выбор из списка, обязательно)
- **Действие**: Создание нового course_instance в статусе "planned"

#### 2.3. Кнопка "Запланировать по графику"
- **Где**:
  - В списке групп курса (рядом с "Запустить курс")
  - В форме "График запуска курса"
- **Действие**: Открытие/редактирование формы launch_schedule для курса

### 3. Условия старта заданий

#### 3.1. Новое условие "После сдачи предыдущего задания"
- **Было**: `start_condition`: course_start / prev_complete / manual
- **Добавить**: `prev_assignment_submitted` - старт после отправки предыдущего задания на проверку
- **Где**: Форма "График заданий" (assignment_schedule) у методиста

### 4. График запуска курса

#### 4.1. Переименование режима запуска
- **Было**: "Вручную администратором"
- **Стало**: "Вручную"
- **Где**: Форма launch_schedule, все упоминания в UI

## Затронутые файлы

### Прототип (`proto/`)

#### Модули (приоритет - методист, затем остальные):
1. **proto/modules/methodist.js**
   - Форма создания/редактирования задания
   - Новый раздел "Группы курса"
   - Модалка "Запустить курс"
   - Форма "График запуска курса"
   - Форма "График заданий"

2. **proto/modules/student.js**
   - Карточки предпросмотра заданий (показывать short_description)
   - Страница просмотра задания (показывать detailed_instruction, видео, файлы)
   - Убрать упоминания submission_type из UI

3. **proto/modules/teacher.js**
   - Карточки предпросмотра заданий (показывать short_description)
   - Страница просмотра задания студента (показывать detailed_instruction)
   - Убрать submission_type из форм проверки

4. **proto/modules/admin.js**
   - Возможно, упоминания групп курсов в управлении (проверить)

#### Данные:
5. **proto/data.js**
   - Обновить mock данные для заданий (добавить short_description, detailed_instruction, instruction_videos, instruction_files)
   - Обновить assignment_schedule (добавить prev_assignment_submitted)
   - Обновить launch_schedule (переименовать manual)

### Документация

6. **doc/03_entities.md**
   - Обновить сущность `assignment_template`:
     - Переименовать `description` → `short_description`
     - Добавить `detailed_instruction` (Text)
     - Добавить `instruction_videos` (JSON)
     - Добавить `instruction_files` (JSON)
   - Обновить сущность `assignment_schedule`:
     - Добавить значение `prev_assignment_submitted` в enum `start_condition`
   - Обновить сущность `launch_schedule`:
     - Переименовать режим "Вручную администратором" → "Вручную"

### ER-диаграмма

7. **proto/er-viewer/data.js**
   - Синхронизировать изменения из doc/03_entities.md:
     - `assignment_template.attributes[]`
     - `assignment_schedule.attributes[]`
     - `launch_schedule.attributes[]`

## Порядок реализации

1. ✅ Создать план задачи
2. 🔄 Описать все изменения (текущий файл)
3. ⏳ Реализовать изменения в methodist.js
4. ⏳ Реализовать изменения в student.js, teacher.js, admin.js
5. ⏳ Обновить proto/data.js
6. ⏳ Обновить doc/03_entities.md
7. ⏳ Обновить proto/er-viewer/data.js
8. ⏳ Проверить консистентность

## Детализация изменений

### methodist.js

**Форма создания/редактирования задания** (`editAssignmentModal`):
```diff
- <label>Описание задания</label>
+ <label>Краткое описание</label>
  <textarea name="description">...

+ <label>Подробная инструкция</label>
+ <textarea name="detailed_instruction" rows="10">...
+
+ <label>Видео для просмотра</label>
+ <input type="url" name="instruction_video" placeholder="Ссылка на YouTube/Vimeo">
+ <button>+ Добавить ещё видео</button>
+
+ <label>Файлы инструкции</label>
+ <input type="file" name="instruction_file" multiple>

- <label>Тип сдачи</label>
- <select name="submission_type">...
```

**Меню "Разделы шаблона"** (при редактировании курса):
```diff
  <button onclick="showCourseSection('general')">Общие сведения</button>
  <button onclick="showCourseSection('assignments')">Задания</button>
  <button onclick="showCourseSection('schedule')">График заданий</button>
  <button onclick="showCourseSection('launch')">График запуска</button>
+ <button onclick="showCourseSection('instances')">Группы курса</button>
```

**Новая секция "Группы курса"**:
```html
<div id="instances-section">
  <h3>Группы курса</h3>
  <div class="actions">
    <button onclick="launchCourseModal()">🚀 Запустить курс</button>
    <button onclick="showLaunchSchedule()">📅 Запланировать по графику</button>
  </div>

  <div class="tabs">
    <button class="active" data-status="planned">Запланированные</button>
    <button data-status="active">Активные</button>
    <button data-status="completed">Завершенные</button>
  </div>

  <table class="instances-list">
    <thead>
      <tr>
        <th>Название группы</th>
        <th>Преподаватель</th>
        <th>Даты</th>
        <th>Студентов</th>
        <th>Статус</th>
      </tr>
    </thead>
    <tbody>
      <!-- Динамическая генерация -->
    </tbody>
  </table>
</div>
```

**Модалка "Запустить курс"**:
```html
<div id="launchCourseModal" class="modal">
  <h2>Запустить курс</h2>
  <form>
    <label>Название группы (опционально)</label>
    <input type="text" name="group_name" placeholder="Например: Группа 01-2025">

    <label>Дата начала *</label>
    <input type="date" name="start_date" required>

    <label>Дата окончания *</label>
    <input type="date" name="end_date" required>

    <label>Преподаватель *</label>
    <select name="instructor_id" required>
      <!-- Список преподавателей -->
    </select>

    <button type="submit">Создать группу</button>
  </form>
</div>
```

**Форма "График запуска курса"**:
```diff
+ <button onclick="showLaunchSchedule()">📅 Запланировать по графику</button>

  <label>Режим запуска</label>
  <select name="launch_mode">
-   <option value="manual">Вручную администратором</option>
+   <option value="manual">Вручную</option>
    <option value="periodic">Периодический</option>
    <option value="dates">Конкретные даты</option>
  </select>
```

**Форма "График заданий"**:
```diff
  <label>Условие старта</label>
  <select name="start_condition">
    <option value="course_start">Начало курса</option>
    <option value="prev_complete">После завершения предыдущего</option>
+   <option value="prev_assignment_submitted">После сдачи предыдущего задания</option>
    <option value="manual">Вручную</option>
  </select>
```

### student.js

**Карточка предпросмотра задания** (в списке заданий):
```diff
  <div class="assignment-card">
    <h4>${assignment.title}</h4>
-   <p>${assignment.description}</p>
+   <p>${assignment.short_description}</p>
    ...
  </div>
```

**Страница просмотра задания**:
```diff
  <div class="assignment-view">
    <h2>${assignment.title}</h2>
+   <p class="short-desc">${assignment.short_description}</p>

+   <div class="detailed-instruction">
+     <h3>Подробная инструкция</h3>
+     <div>${assignment.detailed_instruction}</div>
+
+     ${assignment.instruction_videos?.length > 0 ? `
+       <div class="instruction-videos">
+         <h4>Видео для просмотра</h4>
+         ${assignment.instruction_videos.map(url => `
+           <iframe src="${url}" ...></iframe>
+         `).join('')}
+       </div>
+     ` : ''}
+
+     ${assignment.instruction_files?.length > 0 ? `
+       <div class="instruction-files">
+         <h4>Файлы инструкции</h4>
+         ${assignment.instruction_files.map(file => `
+           <a href="${file.url}" download>${file.name}</a>
+         `).join('')}
+       </div>
+     ` : ''}
+   </div>

-   <p>Тип сдачи: ${assignment.submission_type}</p>

    <form class="submission-form">
      <!-- Форма сдачи без упоминания submission_type -->
    </form>
  </div>
```

### teacher.js

**Карточка предпросмотра задания** (в gradebook, списках):
```diff
  <div class="assignment-card">
    <h4>${assignment.title}</h4>
-   <p>${assignment.description}</p>
+   <p>${assignment.short_description}</p>
    ...
  </div>
```

**Страница проверки задания студента**:
```diff
  <div class="assignment-review">
    <h2>${assignment.title}</h2>
+   <p class="short-desc">${assignment.short_description}</p>

+   <details>
+     <summary>Подробная инструкция задания</summary>
+     <div>${assignment.detailed_instruction}</div>
+     <!-- видео и файлы, если есть -->
+   </details>

-   <p>Тип сдачи: ${assignment.submission_type}</p>

    <div class="student-submission">
      <!-- Работа студента -->
    </div>

    <form class="grading-form">
      <!-- Форма оценивания без submission_type -->
    </form>
  </div>
```

### admin.js

Проверить, нет ли упоминаний `submission_type` в формах управления. Если есть - удалить.

### data.js

**Обновить mock данные**:
```js
const assignmentTemplates = [
  {
    id: "assignment-1",
    course_template_id: "course-1",
    title: "Основы SQL",
-   description: "Изучите базовый синтаксис SQL...",
+   short_description: "Изучите базовый синтаксис SQL: SELECT, WHERE, JOIN",
+   detailed_instruction: `
+     <h3>Цели задания</h3>
+     <p>В этом задании вы научитесь...</p>
+     <h3>Шаги выполнения</h3>
+     <ol>
+       <li>Подключитесь к базе данных</li>
+       <li>Выполните запросы...</li>
+     </ol>
+   `,
+   instruction_videos: [
+     "https://www.youtube.com/embed/abc123"
+   ],
+   instruction_files: [
+     { name: "schema.sql", url: "/files/schema.sql" },
+     { name: "sample_data.csv", url: "/files/sample_data.csv" }
+   ],
    submission_type: ["text", "file"], // оставляем в данных, но не показываем в UI
    ...
  }
];

const assignmentSchedules = [
  {
    id: "sched-1",
    assignment_template_id: "assignment-1",
    start_condition: "course_start", // или "prev_complete", "prev_assignment_submitted", "manual"
    ...
  }
];

const launchSchedules = [
  {
    id: "launch-1",
    course_template_id: "course-1",
    launch_mode: "manual", // было "manual_admin"
    ...
  }
];
```

## Проверка консистентности

После всех изменений проверить:

1. **Терминология**:
   - Везде "Краткое описание", а не "Описание"
   - Везде "Подробная инструкция", а не "Полное описание"
   - Везде "Вручную", а не "Вручную администратором"

2. **Данные**:
   - proto/data.js содержит все новые поля
   - doc/03_entities.md отражает актуальную модель
   - proto/er-viewer/data.js синхронизирован с doc/03_entities.md

3. **UI**:
   - submission_type нигде не отображается
   - short_description показывается в карточках
   - detailed_instruction показывается на странице задания
   - Видео и файлы корректно отображаются

4. **Функционал**:
   - Методист может запустить курс вручную
   - Методист может запланировать по графику
   - Условие "После сдачи предыдущего" доступно в графике заданий

## Ожидаемые результаты

- ✅ Задания имеют краткое и подробное описание
- ✅ Подробная инструкция может содержать видео и файлы
- ✅ Тип сдачи скрыт из всех форм
- ✅ Методист может управлять группами курса через новый раздел меню
- ✅ Методист может запустить курс вручную через модалку
- ✅ Методист может запланировать запуск по графику
- ✅ Доступно новое условие старта задания "После сдачи предыдущего"
- ✅ Документация и ER-диаграмма синхронизированы с прототипом

## Риски и ограничения

- **Миграция данных**: При переходе на реальную систему потребуется миграция `description` → `short_description`
- **Обратная совместимость**: Если `short_description` пустое, показывать первые 100 символов `detailed_instruction`
- **Валидация**: Поле `short_description` рекомендовано (не обязательно), `detailed_instruction` обязательно
